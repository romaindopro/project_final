<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
        
	</head>
	
    <body>
		<h1>Demande de
			{{ contact.name }}</h1>

		<p>Email :
			{{ contact.email }}</p>
		
		<p class="message">Message :</p>
		<div>
			{{ contact.message|raw }}
		</div>
	</body>

</html>

