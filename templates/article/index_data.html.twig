{% for article in articles %}
	<div class="box">
		<div class="image">
			<img src="/upload/image/{{article.image }}" alt="img">
		</div>
		<div class="content">
			<h3>{{ article.title }}</h3>
			<p>{{ article.content|smart_short_txt }}</p>
			
			<a href="book.php" class="btn">book now</a>
			<span>
				publié le :
				{{ article.publishedDate ? article.publishedDate|date('d-m-Y') : ''}}
			</span>
		</div>
	</div>
{% endfor %}
