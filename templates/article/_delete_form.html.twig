<form method="post" action="{{ path('app_article_delete', {'id': articles.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ articles.id) }}">
    <button class="btn">Effacer l'article</button>
</form>
